@page
@model KitapYorum.RazorPage.Pages.Roller.IndexModel
@{
    Model.MyProp = "Deneme";  
}
@* <form  method="post">

    <label asp-for="MyProp"> Birseyler giriniz</label>
    <input type="text" asp-for="MyProp" value="@Model.MyProp"/>

    <button type="submit"> Kaydet</button>
</form> *@

<a class="btn btn-primary" asp-page="Create">Yeni Role Ekle</a>
<a class="btn btn-primary" asp-page="/Kategoriler/Index">Kategoriler</a>

<table class="table table-hover">
    <thead>
        <tr>
            <th>ID</th>
            <th>Role Name</th>

        </tr>
    </thead>
    <tbody>
        @foreach(var role in Model.Roller)
        {
            <tr>
                <td>@role.Id</td>
                <td>@role.Name</td>

            </tr>
        }
    </tbody>
</table>

